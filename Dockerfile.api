# API Server Dockerfile for Found Footy and other backend services
FROM node:18-alpine

WORKDIR /app

# Copy package files
COPY package.json package-lock.json* ./

# Install ALL dependencies (including dev for tsx)
RUN npm ci

# Copy source code
COPY src/server ./src/server

# Production settings
ENV NODE_ENV=production

EXPOSE 3001

# Run the API server
CMD ["npx", "tsx", "src/server/index.ts"]
